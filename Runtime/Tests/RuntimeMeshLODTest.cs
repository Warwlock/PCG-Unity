using UnityEngine;

public class RuntimeMeshLODTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        var mesh = new Mesh();
        MeshLodRange meshLodRange = new MeshLodRange();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
